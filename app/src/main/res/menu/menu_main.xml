<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.boommates.boommates.MainActivity">
    <group
        android:id="@+id/admin_menu_group"
        android:visible="false">
        <item
            android:id="@+id/action_edit_tasks"
            android:orderInCategory="100"
            android:title="@string/menu_edit_tasks"
            app:showAsAction="never" />
        <item
            android:id="@+id/action_remove_members"
            android:orderInCategory="101"
            android:title="@string/menu_remove_members"
            app:showAsAction="never" />
    </group>
    <item
        android:id="@+id/action_account_settings"
        android:icon="@drawable/settings"
        android:orderInCategory="102"
        android:title="@string/menu_account_settings"
        app:showAsAction="ifRoom" />
</menu>
